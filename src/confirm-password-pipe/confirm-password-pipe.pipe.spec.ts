import { ConfirmPasswordPipePipe } from './confirm-password-pipe.pipe';

describe('ConfirmPasswordPipePipe', () => {
  it('should be defined', () => {
    expect(new ConfirmPasswordPipePipe()).toBeDefined();
  });
});

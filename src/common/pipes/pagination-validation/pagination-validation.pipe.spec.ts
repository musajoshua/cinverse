import { PaginationValidationPipe } from './pagination-validation.pipe';

describe('PaginationValidationPipe', () => {
  it('should be defined', () => {
    expect(new PaginationValidationPipe()).toBeDefined();
  });
});
